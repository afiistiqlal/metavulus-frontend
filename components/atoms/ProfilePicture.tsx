import React from 'react'
import Image from "next/image";

type Props = {}

const ProfilePicture = (props: Props) => {
  return (
    <div className="w-10 h-10 rounded-full border bg-mv-white-4">{/* <Image /> */}</div>
  )
}

export default ProfilePicture
import EventsPageLayout from "@/components/templates/EventsPageLayout";
import React from "react";

type Props = {};

const Events = (props: Props) => {
  return <EventsPageLayout />;
};

export default Events;
